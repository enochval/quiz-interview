{
  "name": "backend",
  "version": "1.0.0",
  "description": "interview test",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf build && mkdir build",
    "build-babel": "babel -d ./build ./src -s",
    "build": "npm run clean && npm run build-babel && cp -r ./src/database ./build",
    "start": "npm run build && node ./build/index.js"
  },
  "author": "Osarenren Enoch",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "joi": "^14.3.1",
    "lodash": "^4.17.11",
    "morgan": "^1.9.1",
    "mz": "^2.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0"
  }
}
